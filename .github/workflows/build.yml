name: Compilar y Publicar Release

on:
  push:
    tags:
      - 'v*' # Se activa cuando subes un tag como v1.0, v2.1.3, etc.
    branches:
      - main # También se activará al hacer push a main (opcional)

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout del código
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12.3' 
          cache: 'pip'

      - name: Instalar dependencias
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install nuitka

      - name: Compilar con Nuitka
        run: |
          python -m nuitka `
            --standalone `
            --onefile `
            --include-package=polars `
            --include-package-data=polars `
            --enable-plugin=anti-bloat `
            --assume-yes-for-downloads `
            --output-dir=dist `
            ./main.py

      - name: Crear Release y Subir Ejecutable
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: dist/*.exe
          name: Release ${{ github.ref_name }}
          generate_release_notes: true
          draft: false
          prerelease: false